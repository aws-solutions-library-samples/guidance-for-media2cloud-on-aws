{
  "name": "mime-bundle",
  "version": "2.0.0",
  "description": "build mime for browser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"bundle exists\" && exit 0",
    "browserify": "browserify index.js -o mime.js",
    "uglify": "uglifyjs mime.js -o mime.min.js; ls -d $PWD/*",
    "copy": "cp -v ./mime*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mime": "^2.3.1"
  },
  "devDependencies": {
  }
}
